{%extends 'base.njk'%}

{%block content%}
<div class="row">
{%for album in albumArr%}
    {{item(album.name)}}
  {%endfor%}
</div>
{%endblock%}

{%macro item(names)%}
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <img src="/images/wjj.jpeg" alt="...">
      <div class="caption">
        <h3>{{names}}</h3>
        <p><a href="{{names}}" class="btn btn-primary" role="button">点击进入</a></p>
      </div>
    </div>
  </div>
{%endmacro%}